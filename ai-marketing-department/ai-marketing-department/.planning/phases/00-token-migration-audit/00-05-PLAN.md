---
phase: 00-token-migration-audit
plan: 05
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - components/ui/button/button.tsx
  - components/ui/accordion/accordion.tsx
  - components/ui/timeline/timeline.tsx
  - components/ui/Toast.tsx
  - components/ui/Skeleton.tsx
  - components/ui/FilterTabs.tsx
  - components/ui/ProductTour.tsx
  - components/ui/OfflineBanner.tsx
  - components/ui/SessionTimeout.tsx
  - components/ui/KeyboardShortcutsHelp.tsx
  - components/charts/LineChart.tsx
  - components/charts/BarChart.tsx
  - components/charts/AreaChart.tsx
  - components/charts/DonutChart.tsx
  - components/charts/ChartContainer.tsx
  - components/charts/ChartTooltip.tsx
  - components/charts/Sparkline.tsx
  - components/layout/LayoutShell.tsx
  - components/layout/Sidebar.tsx
  - components/layout/MobileNav.tsx
  - components/layout/UserMenu.tsx
  - components/layout/BrandSwitcher.tsx
  - components/layout/NavGroup.tsx
  - components/analytics/AnalyticsOverview.tsx
  - components/analytics/PerformanceMetrics.tsx
  - components/analytics/ChartDashboard.tsx
  - components/analytics/ContentInsightsPanel.tsx
  - app/(dashboard)/analytics/page.tsx
autonomous: true

must_haves:
  truths:
    - "All UI primitive components use CSS variable tokens"
    - "All chart components use CSS variable tokens for backgrounds and borders (chart data colors may remain hardcoded)"
    - "All layout shell components use CSS variable tokens"
    - "All analytics components use CSS variable tokens"
    - "Dark mode works correctly on analytics page"
  artifacts:
    - path: "components/ui/button/button.tsx"
      provides: "Button component with CSS variable theming"
      contains: "var(--"
    - path: "components/layout/LayoutShell.tsx"
      provides: "Layout shell with CSS variable theming"
      contains: "var(--"
  key_links:
    - from: "components/ui/*.tsx"
      to: "app/globals.css"
      via: "CSS variable references"
      pattern: "var\\(--"
---

<objective>
Migrate UI primitives (14 files, ~82 instances), chart components (7 files, ~16 instances), layout components (7 files, ~36 instances), and analytics components (4 files, ~42 instances) from hardcoded Tailwind colors to CSS variable tokens.

Purpose: UI primitives and layout are the foundation consumed by all pages. Chart and analytics components complete the data visualization pages. Migrating these ensures the shell and shared components are theme-consistent.

Output: All ui/, charts/, layout/, and analytics/ components use semantic CSS variables.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-token-migration-audit/00-RESEARCH.md
@.planning/phases/00-token-migration-audit/00-01-SUMMARY.md
@app/globals.css
@components/strategy/StrategyPerformancePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate UI primitives and layout components to CSS variable tokens</name>
  <files>
    components/ui/button/button.tsx
    components/ui/accordion/accordion.tsx
    components/ui/timeline/timeline.tsx
    components/ui/Toast.tsx
    components/ui/Skeleton.tsx
    components/ui/FilterTabs.tsx
    components/ui/ProductTour.tsx
    components/ui/OfflineBanner.tsx
    components/ui/SessionTimeout.tsx
    components/ui/KeyboardShortcutsHelp.tsx
    components/layout/LayoutShell.tsx
    components/layout/Sidebar.tsx
    components/layout/MobileNav.tsx
    components/layout/UserMenu.tsx
    components/layout/BrandSwitcher.tsx
    components/layout/NavGroup.tsx
  </files>
  <action>
Apply the standard migration mapping to all UI primitive and layout component files.

**Use the SAME migration mapping established in Plans 02-04.**

**UI-specific notes:**
- button.tsx is a shadcn/ui-style component. It may use variant-based classes. Migrate the variant definitions to use CSS variables. E.g., `variant: "default"` bg → `bg-[var(--button-primary-bg)]`, text → `text-[var(--button-primary-text)]`.
- Skeleton.tsx likely uses `bg-stone-200 animate-pulse` — replace with `bg-[var(--surface-2)]`.
- FilterTabs.tsx active tab state should use accent tokens.
- Toast.tsx may use colored backgrounds for success/error/warning — use semantic tokens.

**Layout-specific notes:**
- Sidebar.tsx and LayoutShell.tsx use sidebar-specific tokens already defined (--sidebar-bg, --sidebar-border, --sidebar-text, etc.). Keep using those. Only migrate non-sidebar hardcoded colors.
- MobileNav.tsx is likely similar to Sidebar — use sidebar tokens.
- UserMenu.tsx dropdown should use `--card-bg` and `--border`.
- BrandSwitcher.tsx dropdown should use `--card-bg` and `--border`.
  </action>
  <verify>
1. `grep -r "bg-white\|text-stone-\|border-gray-\|bg-stone-\|text-gray-\|border-stone-\|bg-gray-" components/ui/ components/layout/ --include="*.tsx" | wc -l` — should be 0
2. `npm run build` — should succeed
  </verify>
  <done>
All UI primitive and layout component files use CSS variable tokens. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate chart and analytics components to CSS variable tokens</name>
  <files>
    components/charts/LineChart.tsx
    components/charts/BarChart.tsx
    components/charts/AreaChart.tsx
    components/charts/DonutChart.tsx
    components/charts/ChartContainer.tsx
    components/charts/ChartTooltip.tsx
    components/charts/Sparkline.tsx
    components/analytics/AnalyticsOverview.tsx
    components/analytics/PerformanceMetrics.tsx
    components/analytics/ChartDashboard.tsx
    components/analytics/ContentInsightsPanel.tsx
    app/(dashboard)/analytics/page.tsx
  </files>
  <action>
Apply the standard migration mapping to chart and analytics components.

**Chart-specific notes:**
- Recharts components receive colors as JavaScript props (e.g., `stroke="#ea580c"`). These are NOT Tailwind classes and should be converted to `var(--accent)` using CSS variable syntax in Recharts: `stroke="var(--accent)"` or using `getComputedStyle` if Recharts doesn't support CSS variables directly. Test this — some Recharts versions support CSS variables in stroke/fill props, some don't. If they don't, leave hex values and add a TODO comment.
- ChartContainer.tsx and ChartTooltip.tsx likely use Tailwind classes for wrapper/tooltip styling — migrate those.
- Chart backgrounds (bg-white card wrapping charts) → `bg-[var(--card-bg)]`.

**Analytics-specific notes:**
- ContentInsightsPanel has 32 instances — highest in analytics.
- AnalyticsOverview likely has metric cards — use `--card-bg`, `--text-primary`, etc.
- Analytics page may have date range selectors — use `--input-bg`, `--input-border`.
  </action>
  <verify>
1. `grep -r "bg-white\|text-stone-\|border-gray-\|bg-stone-\|text-gray-\|border-stone-\|bg-gray-" components/charts/ components/analytics/ app/\(dashboard\)/analytics/ --include="*.tsx" | wc -l` — should be 0
2. `npm run build` — should succeed
  </verify>
  <done>
All chart and analytics component files use CSS variable tokens. Recharts data colors either use CSS variables or are documented with TODO. Build succeeds. Combined with Task 1, this plan eliminates ~176 hardcoded instances.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "bg-white\|text-stone-\|border-gray-\|bg-stone-\|text-gray-\|border-stone-\|bg-gray-" components/ui/ components/charts/ components/layout/ components/analytics/ app/\(dashboard\)/analytics/ --include="*.tsx" | wc -l` returns 0
2. `npm run build` passes
3. Analytics page at localhost:3003/analytics renders correctly in both modes
4. Sidebar and layout shell render correctly after migration
</verification>

<success_criteria>
- Zero hardcoded color utilities in UI, chart, layout, and analytics components
- Button variants use CSS variable tokens
- Layout shell sidebar tokens properly referenced
- Build succeeds without errors
- ~176 hardcoded instances eliminated
</success_criteria>

<output>
After completion, create `.planning/phases/00-token-migration-audit/00-05-SUMMARY.md`
</output>
