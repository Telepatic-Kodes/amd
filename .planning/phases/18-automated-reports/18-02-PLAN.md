---
phase: 18-automated-reports
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - ai-marketing-department/ai-marketing-department/app/(dashboard)/settings/page.tsx
  - ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx
  - ai-marketing-department/ai-marketing-department/components/reports/ReportSettings.tsx
  - ai-marketing-department/ai-marketing-department/components/reports/ReportHistory.tsx
  - ai-marketing-department/ai-marketing-department/components/reports/ReportPreview.tsx
  - ai-marketing-department/ai-marketing-department/lib/language.ts
autonomous: true

must_haves:
  truths:
    - "User can toggle email delivery on/off in settings"
    - "User can view monthly analytics summary with trends in dashboard"
    - "User can see report history in analytics page"
    - "User can preview a past report inline"
    - "User can manually trigger report generation with one click"
    - "All new UI labels in Spanish"
    - "Mobile responsive"
    - "Toast notifications for report generation and email status"
  artifacts:
    - path: "ai-marketing-department/ai-marketing-department/components/reports/ReportSettings.tsx"
      provides: "Report delivery settings (email toggle, frequency selector)"
    - path: "ai-marketing-department/ai-marketing-department/components/reports/ReportHistory.tsx"
      provides: "Report history timeline with status indicators"
    - path: "ai-marketing-department/ai-marketing-department/components/reports/ReportPreview.tsx"
      provides: "Inline report viewer with metrics summary and HTML content"
  key_links:
    - from: "ReportSettings.tsx"
      to: "api.reports.updateReportSettings"
      via: "useMutation for settings changes"
      pattern: "useMutation.*updateReportSettings"
    - from: "ReportHistory.tsx"
      to: "api.reports.getReportHistory"
      via: "useQuery for report list"
      pattern: "useQuery.*getReportHistory"
    - from: "ReportPreview.tsx"
      to: "api.reports.getReportById"
      via: "useQuery for full report content"
      pattern: "useQuery.*getReportById"
---

<objective>
Build the frontend for automated reports: report settings in settings page, report history in analytics page, report preview modal, manual generation button, and download capability.

Purpose: Users need visible controls to manage report delivery and view generated reports — the backend from Plan 01 is invisible without these interfaces.

Output: ReportSettings component in settings, ReportHistory + ReportPreview components in analytics page, Spanish UI.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/18-automated-reports/18-01-SUMMARY.md

@ai-marketing-department/ai-marketing-department/app/(dashboard)/settings/page.tsx
@ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx
@ai-marketing-department/ai-marketing-department/lib/language.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Report settings UI + language keys</name>
  <files>
    ai-marketing-department/ai-marketing-department/components/reports/ReportSettings.tsx
    ai-marketing-department/ai-marketing-department/app/(dashboard)/settings/page.tsx
    ai-marketing-department/ai-marketing-department/lib/language.ts
  </files>
  <action>
1. In lib/language.ts, add new report-related keys:
   ```
   reports: "Reportes"
   reportSettings: "Configuracion de reportes"
   emailDelivery: "Envio por correo"
   emailDeliveryDesc: "Recibe reportes automaticos por correo electronico"
   reportFrequency: "Frecuencia"
   weekly: "Semanal"
   monthly: "Mensual"
   both: "Ambos"
   includeAiInsights: "Incluir analisis de IA"
   includeAiInsightsDesc: "El CMO genera un resumen ejecutivo con recomendaciones"
   recipientEmail: "Correo de destino"
   recipientEmailDesc: "Dejar vacio para usar tu correo de cuenta"
   generateNow: "Generar ahora"
   generating: "Generando..."
   reportGenerated: "Reporte generado exitosamente"
   reportError: "Error al generar reporte"
   reportHistory: "Historial de reportes"
   noReports: "Aun no hay reportes generados"
   viewReport: "Ver reporte"
   downloadReport: "Descargar reporte"
   weeklyReport: "Reporte semanal"
   monthlyReport: "Reporte mensual"
   emailSent: "Correo enviado"
   emailNotSent: "Correo no enviado"
   period: "Periodo"
   settingsSaved: "Configuracion guardada"
   ```

2. Create components/reports/ReportSettings.tsx:
   - Uses `useQuery(api.reports.getUserReportSettings)` for current settings
   - Uses `useMutation(api.reports.updateReportSettings)` for changes
   - Uses `useAction(api.reports.generateReportNow)` for manual trigger
   - Layout: Card with sections:
     a) Email toggle: Switch with label "Envio por correo" + description
     b) Frequency selector: Radio group (Semanal / Mensual / Ambos)
     c) AI insights toggle: Switch "Incluir analisis de IA"
     d) Recipient email: Input field (optional, placeholder "tu@correo.com")
     e) Manual trigger: Two buttons "Generar semanal" and "Generar mensual"
   - On toggle/select change: auto-save via mutation, toast "Configuracion guardada"
   - On generate: call generateReportNow, loading state, toast on completion
   - Mobile responsive: full width inputs, stacked layout

3. In settings/page.tsx:
   - Add new category: { id: "reports", label: "Reportes", icon: FileText }
   - Render ReportSettings component when "reports" category selected
   - Show for all authenticated users (not admin-only)
   - Import FileText from lucide-react, ReportSettings component
  </action>
  <verify>
    Run `cd /home/tomas/Escritorio/AIAIAI_Consulting/projects/amd/ai-marketing-department/ai-marketing-department && npx tsc --noEmit 2>&1 | tail -20` — check for errors.
  </verify>
  <done>
    ReportSettings component with email toggle, frequency selector, AI insights toggle, manual generation buttons. Settings page has "Reportes" category tab. All labels in Spanish.
  </done>
</task>

<task type="auto">
  <name>Task 2: Report history + preview in analytics page</name>
  <files>
    ai-marketing-department/ai-marketing-department/components/reports/ReportHistory.tsx
    ai-marketing-department/ai-marketing-department/components/reports/ReportPreview.tsx
    ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx
  </files>
  <action>
1. Create components/reports/ReportHistory.tsx:
   - Props: { onViewReport: (reportId: Id) => void }
   - Uses `useQuery(api.reports.getReportHistory, { limit: 20 })`
   - Layout: Card with title "Historial de reportes"
   - List of reports (newest first):
     - Each row: Report type badge (Semanal/Mensual), title, period dates, email status indicator, "Ver" button
     - Type badge colors: weekly = blue, monthly = purple
     - Email status: green check (sent), amber warning (not sent)
     - Dates formatted in Spanish (es-ES locale)
   - Empty state: "Aun no hay reportes generados. Configura reportes automaticos en Ajustes."
   - Loading: Skeleton rows
   - Mobile: Stack badges vertically, compact layout

2. Create components/reports/ReportPreview.tsx:
   - Props: { reportId: Id<"reports">, onClose: () => void }
   - Uses `useQuery(api.reports.getReportById, { id: reportId })`
   - Modal/slide-over panel:
     a) Header: Report title + type badge + close button
     b) Metrics summary: 4 cards (Contenido publicado, Tokens usados, Costo, Tasa de exito)
     c) AI Narrative section (if present): Card with narrative text
     d) Full HTML content: Rendered in a sandboxed iframe using srcdoc attribute
        - Use `<iframe sandbox="" srcdoc={report.htmlContent} />` for safe HTML rendering
        - The sandbox attribute prevents script execution, form submission, and other XSS vectors
        - Style the iframe to match the report width and auto-height
     e) Footer: "Descargar reporte" button + "Cerrar" button
   - Download: Create a Blob from HTML content, trigger download as .html file
   - Loading: Full skeleton with placeholder cards
   - Dark theme consistent with existing UI

3. In analytics/page.tsx, add report history section:
   - Read the current file first to understand existing layout
   - Add a new section at the bottom of the page (after existing analytics charts):
     - Section title: "Reportes" with FileText icon
     - Render ReportHistory component
   - When user clicks "Ver": show ReportPreview modal
   - State: useState for selectedReportId (null = hidden, Id = show preview)
   - Add appropriate imports
  </action>
  <verify>
    Run `cd /home/tomas/Escritorio/AIAIAI_Consulting/projects/amd/ai-marketing-department/ai-marketing-department && npx tsc --noEmit 2>&1 | tail -20` — must compile without errors.
  </verify>
  <done>
    ReportHistory shows timeline of past reports in analytics page. ReportPreview displays full report in sandboxed iframe with metrics cards. Download button exports report as HTML file. All Spanish labels. Mobile responsive.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles clean (frontend)
2. Settings page has "Reportes" tab with email toggle, frequency, AI insights, manual generation
3. Analytics page shows "Historial de reportes" section
4. Report preview modal displays metrics summary and sandboxed HTML content
5. Manual "Generar ahora" button triggers report generation with loading state
6. Toast notifications for settings changes, report generation, and errors
7. All labels in Spanish
8. Mobile responsive at 375px width
</verification>

<success_criteria>
- User can toggle email delivery on/off in settings
- User can choose report frequency (weekly/monthly/both)
- User can manually trigger report generation
- User can view report history in analytics page
- User can preview full report inline (sandboxed iframe)
- User can download report as HTML file
- All UI in Spanish
- Mobile responsive
- Toast notifications for all actions
</success_criteria>

<output>
After completion, create `.planning/phases/18-automated-reports/18-02-SUMMARY.md`
</output>
