---
phase: 05-design-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-marketing-department/ai-marketing-department/components/layout/Sidebar.tsx
  - ai-marketing-department/ai-marketing-department/components/layout/LayoutShell.tsx
  - ai-marketing-department/ai-marketing-department/components/layout/MobileNav.tsx
  - ai-marketing-department/ai-marketing-department/app/(dashboard)/page.tsx
  - ai-marketing-department/ai-marketing-department/app/(dashboard)/results/page.tsx
  - ai-marketing-department/ai-marketing-department/app/(dashboard)/content/page.tsx
  - ai-marketing-department/ai-marketing-department/components/ui/Button.tsx
  - ai-marketing-department/ai-marketing-department/components/ui/Card.tsx
  - ai-marketing-department/ai-marketing-department/components/feeds/FeedCard.tsx
autonomous: true

must_haves:
  truths:
    - "Interface is fully usable on mobile at 375px width"
    - "Touch targets are large enough to tap easily (44x44px minimum)"
    - "Bottom navigation bar appears on mobile devices"
    - "Content displays in single column on mobile screens"
    - "Text is readable on small screens (minimum 16px body text)"
  artifacts:
    - path: "ai-marketing-department/ai-marketing-department/components/layout/MobileNav.tsx"
      provides: "Bottom navigation bar for mobile"
      min_lines: 50
    - path: "ai-marketing-department/ai-marketing-department/components/layout/Sidebar.tsx"
      provides: "Responsive sidebar (hidden on mobile)"
      contains: "md:flex"
    - path: "ai-marketing-department/ai-marketing-department/components/layout/LayoutShell.tsx"
      provides: "Responsive layout shell with mobile padding"
      contains: "pb-20"
  key_links:
    - from: "ai-marketing-department/ai-marketing-department/components/layout/MobileNav.tsx"
      to: "ai-marketing-department/ai-marketing-department/components/layout/LayoutShell.tsx"
      via: "Imported and rendered in layout"
      pattern: "import.*MobileNav|<MobileNav"
    - from: "ai-marketing-department/ai-marketing-department/components/layout/Sidebar.tsx"
      to: "ai-marketing-department/ai-marketing-department/components/layout/LayoutShell.tsx"
      via: "Hidden on mobile with md:flex"
      pattern: "hidden md:flex"
---

<objective>
Implement comprehensive mobile responsiveness with touch-friendly interactions, bottom navigation, and single-column layouts for small screens.

Purpose: Make the AMD dashboard fully usable on mobile devices (phones and tablets), ensuring non-technical users can manage their marketing department from any device.

Output: Fully responsive interface with bottom mobile navigation, optimized touch targets (44x44px+), single-column card layouts on mobile, and readable typography on small screens.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile bottom navigation component</name>
  <files>ai-marketing-department/ai-marketing-department/components/layout/MobileNav.tsx</files>
  <action>
Create a new MobileNav component for mobile bottom navigation (MOBILE-03):

1. Create file at components/layout/MobileNav.tsx with:
   ```tsx
   "use client";

   import Link from "next/link";
   import { usePathname } from "next/navigation";
   import { Home, FileText, BarChart3, Settings } from "lucide-react";
   import { cn } from "@/lib/utils";
   import { translate } from "@/lib/language";
   ```

2. Define navigation items matching Sidebar (4 main items):
   - Inicio (Home) - icon: Home
   - Contenido (Content) - icon: FileText
   - Resultados (Analytics) - icon: BarChart3
   - Configuración (Settings) - icon: Settings

3. Implement bottom nav bar:
   - Fixed position at bottom: `fixed bottom-0 left-0 right-0 z-50`
   - Background: `bg-zinc-950/95 backdrop-blur-xl border-t border-zinc-800`
   - Height: `h-20` (80px = generous tap area)
   - Grid layout: `grid grid-cols-4` (4 equal columns)
   - Only visible on mobile: `md:hidden` (hidden on desktop)

4. Navigation item styling:
   - Each item: Flex column with icon on top, label below
   - Touch targets: `min-h-[44px] min-w-[44px]` (WCAG standard - MOBILE-02)
   - Icon size: `h-6 w-6` (24px - clear and tappable)
   - Text size: `text-xs` (12px label under icon)
   - Active state: `text-indigo-400` with subtle background `bg-indigo-500/10`
   - Inactive state: `text-zinc-500` with hover `hover:text-zinc-300`
   - Padding: `p-3` around each item for comfortable tapping

5. Use usePathname for active state detection (same logic as Sidebar)

6. Export default function MobileNav
  </action>
  <verify>
```bash
cd /home/tomas/Escritorio/amd/ai-marketing-department/ai-marketing-department
test -f components/layout/MobileNav.tsx && echo "MobileNav created"
grep "min-h-\[44px\]" components/layout/MobileNav.tsx
grep "md:hidden" components/layout/MobileNav.tsx
grep "grid grid-cols-4" components/layout/MobileNav.tsx
```
  </verify>
  <done>MobileNav.tsx exists with bottom navigation bar, 44x44px touch targets, 4-column grid, hidden on desktop (md:hidden)</done>
</task>

<task type="auto">
  <name>Task 2: Update layout components for responsive behavior</name>
  <files>
ai-marketing-department/ai-marketing-department/components/layout/Sidebar.tsx
ai-marketing-department/ai-marketing-department/components/layout/LayoutShell.tsx
  </files>
  <action>
Update Sidebar and LayoutShell for mobile responsiveness (MOBILE-01):

**For Sidebar.tsx:**

1. Hide sidebar on mobile, show on desktop:
   - Change root div class from `flex h-screen w-64 flex-col fixed` to `hidden md:flex h-screen w-64 flex-col fixed`
   - This hides sidebar on mobile (<768px) and shows on desktop (≥768px)

2. Keep all existing functionality (navigation items, tooltips, Spanish labels)

**For LayoutShell.tsx (create or update):**

1. If LayoutShell.tsx doesn't exist, check how layout is currently structured:
   - Likely in app/(dashboard)/layout.tsx
   - If so, update app/(dashboard)/layout.tsx instead

2. Update the main layout structure:
   - Sidebar wrapper: Add responsive classes `hidden md:block`
   - Main content area: Adjust left margin for sidebar - `md:ml-64` (only on desktop)
   - Mobile content: Full width on mobile - `ml-0 md:ml-64`
   - Add bottom padding for mobile nav: `pb-20 md:pb-0` (80px bottom padding on mobile, none on desktop)

3. Import and render MobileNav:
   ```tsx
   import { MobileNav } from "@/components/layout/MobileNav";
   ```
   - Add `<MobileNav />` after main content (outside main container)

4. Responsive padding for content:
   - Mobile: `p-4` (16px padding)
   - Desktop: `p-8` (32px padding)
   - Class: `p-4 md:p-8`

5. Keep existing Sidebar import and render
  </action>
  <verify>
```bash
cd /home/tomas/Escritorio/amd/ai-marketing-department/ai-marketing-department
grep "hidden md:flex" components/layout/Sidebar.tsx
# Check if LayoutShell exists or layout is in app/(dashboard)/layout.tsx
test -f components/layout/LayoutShell.tsx && grep "MobileNav" components/layout/LayoutShell.tsx || grep "MobileNav" app/\(dashboard\)/layout.tsx
grep "pb-20 md:pb-0" components/layout/LayoutShell.tsx || grep "pb-20 md:pb-0" app/\(dashboard\)/layout.tsx
```
  </verify>
  <done>Sidebar hidden on mobile (hidden md:flex), layout has bottom padding for mobile nav (pb-20 md:pb-0), MobileNav imported and rendered, content area responsive (ml-0 md:ml-64, p-4 md:p-8)</done>
</task>

<task type="auto">
  <name>Task 3: Optimize components for mobile single-column layouts and touch targets</name>
  <files>
ai-marketing-department/ai-marketing-department/app/(dashboard)/page.tsx
ai-marketing-department/ai-marketing-department/app/(dashboard)/results/page.tsx
ai-marketing-department/ai-marketing-department/app/(dashboard)/content/page.tsx
ai-marketing-department/ai-marketing-department/components/ui/Button.tsx
ai-marketing-department/ai-marketing-department/components/ui/Card.tsx
ai-marketing-department/ai-marketing-department/components/feeds/FeedCard.tsx
  </files>
  <action>
Update page layouts and components for mobile-first responsive design (MOBILE-04, MOBILE-05):

**For all dashboard pages (page.tsx, results/page.tsx, content/page.tsx):**

1. Change grid layouts to single column on mobile:
   - Metric cards grid: Change `grid-cols-1 md:grid-cols-2 lg:grid-cols-4` to `grid-cols-1 lg:grid-cols-3`
   - Content grids: Change to `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
   - General pattern: Start with `grid-cols-1` (mobile), then add breakpoints for larger screens

2. Responsive typography (MOBILE-05):
   - Main headers (h1): `text-3xl md:text-5xl` (48px on desktop, 30px on mobile for readability)
   - Section headers (h2): `text-2xl md:text-3xl`
   - Card headers (h3): `text-xl md:text-2xl`
   - Body text: Keep at `text-base` minimum (16px - already meets requirement)
   - Small text: Never go below `text-sm` (14px)

3. Adjust spacing for mobile:
   - Section gaps: `space-y-6 md:space-y-12` (smaller gaps on mobile)
   - Grid gaps: `gap-4 md:gap-8` (tighter on mobile)

**For Button component:**

1. Ensure touch targets meet WCAG (MOBILE-02):
   - Small size: Already px-4 py-2 (meets 44x44 with padding)
   - Medium size: Already px-6 py-3 (generous touch area)
   - Large size: Already px-8 py-4 (very generous)
   - Add minimum height class: `min-h-[44px]` to all size variants

**For Card component:**

1. Responsive padding:
   - CardHeader: `px-4 py-3 md:px-6 md:py-4` (tighter on mobile)
   - CardContent: `p-4 md:p-6` (tighter on mobile)
   - CardFooter: `px-4 py-3 md:px-6 md:py-4` (tighter on mobile)

**For FeedCard component (if exists):**

1. Ensure card layout stacks vertically on mobile
2. Touch-friendly spacing for interactive elements
3. Readable font sizes (minimum text-base for descriptions)

Keep all existing functionality, Spanish labels, data fetching, and interactions intact.
  </action>
  <verify>
```bash
cd /home/tomas/Escritorio/amd/ai-marketing-department/ai-marketing-department
# Check responsive grids
grep "grid-cols-1" app/(dashboard)/page.tsx
grep "text-3xl md:text-5xl" app/(dashboard)/page.tsx
# Check button touch targets
grep "min-h-\[44px\]" components/ui/Button.tsx
# Check responsive card padding
grep "p-4 md:p-6" components/ui/Card.tsx
# Build verification
npm run build 2>&1 | grep -E "(compiled successfully|error)" | head -5
```
  </verify>
  <done>
All pages have single-column mobile layouts (grid-cols-1), responsive typography (text-3xl md:text-5xl headers), tighter mobile spacing (space-y-6 md:space-y-12). Buttons have min-h-[44px] touch targets. Cards have responsive padding (p-4 md:p-6). Build succeeds without errors. Minimum body text size is 16px (text-base).
  </done>
</task>

</tasks>

<verification>
Visual inspection on mobile:
1. Resize browser to 375px width or use mobile emulator
2. Verify sidebar disappears, bottom navigation appears
3. Tap each bottom nav item - should navigate correctly with adequate touch area
4. All content should display in single column without horizontal scroll
5. Headers should be readable but not overwhelming (smaller than desktop)
6. All buttons should be easily tappable (44x44px minimum)
7. Cards should have appropriate padding for mobile viewing

Technical verification:
```bash
cd /home/tomas/Escritorio/amd/ai-marketing-department/ai-marketing-department
npm run build
# Build should complete without errors

# Verify mobile nav exists
test -f components/layout/MobileNav.tsx && echo "✓ MobileNav created"

# Check responsive classes
grep -r "md:flex" components/layout/Sidebar.tsx && echo "✓ Sidebar responsive"
grep -r "grid-cols-1" app/(dashboard)/*.tsx && echo "✓ Mobile grids"
```
</verification>

<success_criteria>
- [ ] MOBILE-01 met: Fully responsive design works at 375px width
- [ ] MOBILE-02 met: All touch targets minimum 44x44px (buttons, nav items, interactive cards)
- [ ] MOBILE-03 met: Bottom navigation bar visible on mobile (md:hidden), with 4 main items
- [ ] MOBILE-04 met: Single-column layout on mobile (grid-cols-1 for card grids)
- [ ] MOBILE-05 met: Text readable on small screens (minimum 16px body text, responsive headers)
- [ ] No horizontal scroll on mobile devices
- [ ] All navigation works on mobile (bottom nav and page transitions)
- [ ] Build completes without TypeScript errors
- [ ] Desktop experience unchanged (sidebar still visible, layouts work)
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-polish/05-02-SUMMARY.md`
</output>
