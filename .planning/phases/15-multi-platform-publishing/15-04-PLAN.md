---
phase: 15-multi-platform-publishing
plan: 04
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - ai-marketing-department/ai-marketing-department/components/twitter/TwitterConnectionCard.tsx
  - ai-marketing-department/ai-marketing-department/components/twitter/PublishToTwitterButton.tsx
  - ai-marketing-department/ai-marketing-department/components/twitter/TwitterPostPreview.tsx
  - ai-marketing-department/ai-marketing-department/components/twitter/TwitterThreadPreview.tsx
autonomous: true

must_haves:
  truths:
    - "User can see Twitter connection status (connected/expired/disconnected) in a card"
    - "User can connect Twitter account with one click from the connection card"
    - "User can preview how their content looks as a tweet or thread before publishing"
    - "User can publish approved content to Twitter with loading state and success/error feedback"
    - "Thread preview shows numbered tweet chunks with character counts"
  artifacts:
    - path: "ai-marketing-department/ai-marketing-department/components/twitter/TwitterConnectionCard.tsx"
      provides: "Twitter connection status display and connect/disconnect actions"
      min_lines: 80
    - path: "ai-marketing-department/ai-marketing-department/components/twitter/PublishToTwitterButton.tsx"
      provides: "Publish action with preview toggle"
      min_lines: 80
    - path: "ai-marketing-department/ai-marketing-department/components/twitter/TwitterPostPreview.tsx"
      provides: "Single tweet preview (mimics Twitter UI)"
      min_lines: 40
    - path: "ai-marketing-department/ai-marketing-department/components/twitter/TwitterThreadPreview.tsx"
      provides: "Thread preview showing all tweet chunks"
      min_lines: 40
  key_links:
    - from: "components/twitter/TwitterConnectionCard.tsx"
      to: "api.twitter.queries.getConnection"
      via: "useQuery hook"
      pattern: "useQuery.*twitter\\.queries\\.getConnection"
    - from: "components/twitter/PublishToTwitterButton.tsx"
      to: "api.twitter.actions.publishToTwitter"
      via: "useAction hook"
      pattern: "useAction.*twitter\\.actions\\.publishToTwitter"
---

<objective>
Build Twitter/X frontend components: connection card, publish button, tweet preview, and thread preview.

Purpose: Provides the user interface for connecting Twitter, previewing content as tweets/threads, and publishing. All UI in Spanish, responsive, with loading states and toast notifications. Covers TX-01 (connect UI), TX-02 (tweet preview), TX-03 (thread preview), TX-04 (connection status display), TX-05 (publish with history), UX-01 through UX-04.

Output: 4 React components in components/twitter/ directory following the LinkedIn component pattern.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-platform-publishing/15-02-SUMMARY.md

@ai-marketing-department/ai-marketing-department/components/linkedin/LinkedInConnectionCard.tsx
@ai-marketing-department/ai-marketing-department/components/linkedin/PublishToLinkedInButton.tsx
@ai-marketing-department/ai-marketing-department/components/linkedin/LinkedInPostPreview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TwitterConnectionCard and TwitterPostPreview</name>
  <files>ai-marketing-department/ai-marketing-department/components/twitter/TwitterConnectionCard.tsx, ai-marketing-department/ai-marketing-department/components/twitter/TwitterPostPreview.tsx</files>
  <action>
**TwitterConnectionCard.tsx** — Follow LinkedInConnectionCard.tsx pattern exactly:

- "use client" directive
- Props: `{ convexSiteUrl?: string }`
- Use `useQuery(api.twitter.queries.getConnection)` for connection data
- Use `useMutation(api.twitter.mutations.disconnect)` for disconnect
- Use `useToast()` for success/error notifications
- Check URL params on mount for OAuth callback results: `twitter=connected`, `twitter=error`
- Clean URL params after reading

Status config with Twitter brand colors:
- connected: green, CheckCircle2 icon, "Conectado"
- expired: yellow, AlertTriangle icon, "Expirado"
- disconnected: zinc/gray, XCircle icon, "Desconectado"

Display:
- Twitter bird icon (use a div with the X logo or the Twitter icon from lucide — note: lucide has "Twitter" icon. Use it.)
- Brand color: #1DA1F2 (classic Twitter blue) or #000000 (X black). Use #1DA1F2 for recognition.
- Show @username (with @ prefix) when connected
- Show dailyTweetCount / 50 for rate limit indicator
- Show expiry warning if isExpiringSoon
- Connect button redirects to `{convexSiteUrl}/twitter/auth`
- Reconnect button for expired status
- Disconnect button with confirmation dialog
- Loading state while data loads

All text in Spanish:
- "Conectar Twitter/X"
- "Reconectar"
- "Desconectar"
- "Publicaciones hoy: X/50"
- "Token expira en X dias"
- "Conecta tu cuenta de Twitter/X para publicar tweets y threads."

**TwitterPostPreview.tsx** — Mimics Twitter tweet appearance:

- Props: `{ text: string; authorName: string; authorHandle: string; authorAvatar?: string; isThread?: boolean; tweetNumber?: number; totalTweets?: number }`
- Renders a card that looks like a tweet:
  - Author avatar (circle, 40px), name (bold), @handle (gray), timestamp "Ahora"
  - Tweet text body
  - If isThread: show thread indicator "(X/Y)" badge
  - Character count indicator at bottom: green if <=280, yellow 260-280, red if >280
  - Fake interaction bar (reply, retweet, like, share icons — all non-functional, just visual)
- Styling: rounded card, white/dark theme matching app (dark bg), border
- Mobile responsive (full width on small screens)
  </action>
  <verify>Run `cd /home/tomas/Escritorio/AIAIAI_Consulting/projects/amd/ai-marketing-department/ai-marketing-department && npx next build --no-lint 2>&1 | tail -20` to check for TypeScript compilation errors. Alternatively, check with tsc if available.</verify>
  <done>TwitterConnectionCard shows connection status, handles connect/disconnect/reconnect. TwitterPostPreview renders a realistic tweet mockup. All text in Spanish. Responsive design.</done>
</task>

<task type="auto">
  <name>Task 2: Create PublishToTwitterButton and TwitterThreadPreview</name>
  <files>ai-marketing-department/ai-marketing-department/components/twitter/PublishToTwitterButton.tsx, ai-marketing-department/ai-marketing-department/components/twitter/TwitterThreadPreview.tsx</files>
  <action>
**TwitterThreadPreview.tsx** — Shows a thread of tweets:

- Props: `{ tweets: string[]; authorName: string; authorHandle: string; authorAvatar?: string }`
- Renders each tweet using TwitterPostPreview with isThread=true, tweetNumber, totalTweets
- Connects tweets visually with a vertical line between them (like Twitter's thread view)
- Shows total thread count badge at top: "Hilo de X tweets"
- Summary bar at bottom: total characters, estimated reading time
- If tweets.length === 1, renders as single tweet (no thread indicators)

**PublishToTwitterButton.tsx** — Follow PublishToLinkedInButton.tsx pattern:

- "use client" directive
- Props: `{ contentId: Id<"content">; contentBody: string; contentStatus: string; className?: string }`
- Use `useQuery(api.twitter.queries.getConnectionStatus)` for connection check
- Use `useQuery(api.twitter.queries.getPublishHistory, { contentId })` for publish state
- Use `useAction(api.twitter.actions.publishToTwitter)` for publish action
- Use `useToast()` for notifications

States:
- Not eligible (content not approved/scheduled): return null
- Already published: show green CheckCircle2 + "Publicado en Twitter" + timestamp + link icon
- Ready to publish: show Twitter-branded button "Publicar en Twitter/X"

Preview toggle:
- Click button toggles preview panel
- Preview calls a CLIENT-SIDE thread splitter to show how content will be split
  - Implement a simple client-side `splitForPreview(text: string): string[]` function right in this file (or import from a shared util). This is for PREVIEW only — the real splitting happens server-side in convex/twitter/threadSplitter.ts. But for instant preview without API call, replicate the basic logic:
    - If text <= 280 chars: single tweet
    - Else: split on paragraph/sentence/word boundaries accounting for "(X/Y)" suffixes
- If single tweet: show TwitterPostPreview
- If thread: show TwitterThreadPreview with the array of tweets

Publish flow:
- Confirm dialog: "Publicar este contenido en Twitter/X?" (and if thread: "Se publicara como hilo de X tweets")
- isPublishing loading state with Loader2 spinner
- On success: toast "Publicado en Twitter/X" + "Tweet publicado exitosamente"
- On error: toast "Error al publicar" + error message

Warnings (non-blocking):
- If not connected: yellow warning "No hay cuenta de Twitter/X conectada"
- If rate limited (dailyTweetCount >= 50): yellow warning "Limite diario alcanzado"
- If content > 280 chars: info badge "Se publicara como hilo de X tweets"

All text in Spanish. Mobile responsive.
  </action>
  <verify>Run `cd /home/tomas/Escritorio/AIAIAI_Consulting/projects/amd/ai-marketing-department/ai-marketing-department && npx next build --no-lint 2>&1 | tail -20` to verify compilation.</verify>
  <done>PublishToTwitterButton handles the full publish flow with preview, confirmation, loading states, and toast notifications. TwitterThreadPreview shows connected tweets with visual thread line. All in Spanish, responsive.</done>
</task>

</tasks>

<verification>
1. Frontend compiles without TypeScript errors
2. TwitterConnectionCard displays status states (connected, expired, disconnected)
3. PublishToTwitterButton shows/hides preview, handles publish action
4. TwitterPostPreview renders realistic tweet mockup with character counter
5. TwitterThreadPreview shows connected tweets with vertical line
6. All text in Spanish (UX-01)
7. Components are responsive (UX-02)
8. Toast notifications on all actions (UX-03)
9. Loading states on async operations (UX-04)
</verification>

<success_criteria>
- TX-01: Connect button visible in TwitterConnectionCard
- TX-02: Single tweet preview works for short content
- TX-03: Thread preview shows numbered chunks for long content
- TX-04: Connection status with rate limit display
- TX-05: Published state shows with timestamp
- UX-01: 100% Spanish text
- UX-02: Mobile responsive
- UX-03: Toast notifications
- UX-04: Loading states and skeleton screens
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-platform-publishing/15-04-SUMMARY.md`
</output>
