---
phase: 15-multi-platform-publishing
plan: 05
type: execute
wave: 3
depends_on: ["15-03"]
files_modified:
  - ai-marketing-department/ai-marketing-department/components/instagram/InstagramConnectionCard.tsx
  - ai-marketing-department/ai-marketing-department/components/instagram/PublishToInstagramButton.tsx
  - ai-marketing-department/ai-marketing-department/components/instagram/InstagramPostPreview.tsx
  - ai-marketing-department/ai-marketing-department/components/instagram/InstagramCarouselPreview.tsx
autonomous: true

must_haves:
  truths:
    - "User can see Instagram connection status with Facebook Page info in a card"
    - "User can connect Instagram Business account with guidance about Facebook requirement"
    - "User can preview how their content looks as an Instagram post before publishing"
    - "User can publish approved content with an image URL to Instagram"
    - "User sees clear explanation of why Facebook Business account is needed"
  artifacts:
    - path: "ai-marketing-department/ai-marketing-department/components/instagram/InstagramConnectionCard.tsx"
      provides: "Instagram connection status with Facebook Page info"
      min_lines: 100
    - path: "ai-marketing-department/ai-marketing-department/components/instagram/PublishToInstagramButton.tsx"
      provides: "Publish action with image URL input and preview"
      min_lines: 100
    - path: "ai-marketing-department/ai-marketing-department/components/instagram/InstagramPostPreview.tsx"
      provides: "Single post preview mimicking Instagram UI"
      min_lines: 50
    - path: "ai-marketing-department/ai-marketing-department/components/instagram/InstagramCarouselPreview.tsx"
      provides: "Carousel preview with image indicators"
      min_lines: 40
  key_links:
    - from: "components/instagram/InstagramConnectionCard.tsx"
      to: "api.instagram.queries.getConnection"
      via: "useQuery hook"
      pattern: "useQuery.*instagram\\.queries\\.getConnection"
    - from: "components/instagram/PublishToInstagramButton.tsx"
      to: "api.instagram.actions.publishToInstagram"
      via: "useAction hook"
      pattern: "useAction.*instagram\\.actions\\.publishToInstagram"
---

<objective>
Build Instagram frontend components: connection card with Facebook requirement explanation, publish button with image URL input, post preview, and carousel preview.

Purpose: Provides the user interface for connecting Instagram Business accounts, previewing image posts, and publishing. Includes clear guidance about the Facebook Business account requirement. All UI in Spanish, responsive, with loading states and toast notifications. Covers IG-01 through IG-05, UX-01 through UX-04.

Output: 4 React components in components/instagram/ directory following the LinkedIn component pattern.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-platform-publishing/15-03-SUMMARY.md

@ai-marketing-department/ai-marketing-department/components/linkedin/LinkedInConnectionCard.tsx
@ai-marketing-department/ai-marketing-department/components/linkedin/PublishToLinkedInButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InstagramConnectionCard and InstagramPostPreview</name>
  <files>ai-marketing-department/ai-marketing-department/components/instagram/InstagramConnectionCard.tsx, ai-marketing-department/ai-marketing-department/components/instagram/InstagramPostPreview.tsx</files>
  <action>
**InstagramConnectionCard.tsx** — Follow LinkedInConnectionCard pattern with Instagram-specific additions:

- "use client" directive
- Props: `{ convexSiteUrl?: string }`
- Use `useQuery(api.instagram.queries.getConnection)` for connection data
- Use `useMutation(api.instagram.mutations.disconnect)` for disconnect
- Check URL params for OAuth callback: `instagram=connected`, `instagram=error`

Status config with Instagram brand gradient:
- connected: green, CheckCircle2, "Conectado"
- expired: yellow, AlertTriangle, "Expirado"
- disconnected: gray, XCircle, "Desconectado"
- pending_review: blue/info, Clock icon, "Revision pendiente" (for apps awaiting Meta App Review)

Brand styling:
- Instagram gradient: from-[#833AB4] via-[#FD1D1D] to-[#FCAF45] — use for the icon background
- Use lucide "Instagram" icon
- Show @username when connected
- Show linked Facebook Page name: "Pagina: {facebookPageName}"
- Show dailyPostCount / 25 for rate limit
- Show token expiry (60-day cycle)

**Facebook requirement explanation section** (ALWAYS visible, even when connected):
- Info box with blue/indigo border explaining WHY Facebook is needed:
  - "Instagram Business API requiere:"
  - "1. Una cuenta de Instagram de tipo Business o Creator"
  - "2. Vinculada a una Pagina de Facebook"
  - "3. Aprobacion de Meta App Review (60-90 dias)"
  - Link to Instagram help article: "Como convertir a cuenta Business" (https://help.instagram.com/502981923235522)
- When disconnected, this section is prominent
- When connected, this section is collapsed/accordion

**App Review status section** (conditional):
- If status is "pending_review" or if connection is null and we want to guide users:
  - Show info banner: "La app esta pendiente de revision por Meta. Mientras tanto, puedes probar con cuentas de prueba."
  - This helps manage the 60-90 day App Review expectation (critical path item from roadmap)

Connect/disconnect/reconnect buttons following LinkedIn's pattern.
All text in Spanish.

**InstagramPostPreview.tsx** — Mimics Instagram post appearance:

- Props: `{ imageUrl?: string; caption: string; authorUsername: string; authorAvatar?: string; isCarousel?: boolean; imageCount?: number }`
- Renders a card that looks like an Instagram post:
  - Header: avatar circle, username (bold), "..." menu icon
  - Image area: if imageUrl provided, show image; else show gray placeholder with ImageIcon. Aspect ratio 1:1 (square) container.
  - If isCarousel: show dot indicators below image (filled for current, empty for others)
  - Action bar: heart, comment, send, bookmark icons (non-functional, visual only)
  - Caption area: username in bold, then caption text. Truncate at 125 chars with "...mas" link.
  - Character count: show X/2200 at bottom right

Styling: dark bg matching app theme, rounded border, Instagram-like spacing.
Mobile responsive.
  </action>
  <verify>Run `cd /home/tomas/Escritorio/AIAIAI_Consulting/projects/amd/ai-marketing-department/ai-marketing-department && npx next build --no-lint 2>&1 | tail -20` to check compilation.</verify>
  <done>InstagramConnectionCard shows status with Facebook Page info and Business account requirement explanation. InstagramPostPreview renders realistic Instagram post mockup. All Spanish, responsive.</done>
</task>

<task type="auto">
  <name>Task 2: Create PublishToInstagramButton and InstagramCarouselPreview</name>
  <files>ai-marketing-department/ai-marketing-department/components/instagram/PublishToInstagramButton.tsx, ai-marketing-department/ai-marketing-department/components/instagram/InstagramCarouselPreview.tsx</files>
  <action>
**InstagramCarouselPreview.tsx** — Shows carousel preview:

- Props: `{ imageUrls: string[]; caption: string; authorUsername: string; authorAvatar?: string }`
- Renders like InstagramPostPreview but with carousel features:
  - Left/right navigation arrows overlaying the image
  - State: currentIndex to track which image is shown
  - Dot indicators below image showing position
  - Caption shared across all images (Instagram carousel uses single caption)
  - Image counter: "1 de X" in top right corner
  - If imageUrls is empty, show placeholder
- Mobile: swipeable would be ideal but not required — arrows are sufficient

**PublishToInstagramButton.tsx** — Follow PublishToLinkedInButton pattern with image handling:

- "use client" directive
- Props: `{ contentId: Id<"content">; contentBody: string; contentStatus: string; className?: string }`
- Use `useQuery(api.instagram.queries.getConnectionStatus)` for connection check
- Use `useQuery(api.instagram.queries.getPublishHistory, { contentId })` for publish state
- Use `useAction(api.instagram.actions.publishToInstagram)` for publish action
- Use `useToast()` for notifications

States (same pattern as LinkedIn):
- Not eligible: return null
- Already published: green badge + timestamp + link
- Ready: show Instagram-branded button

**Key difference from LinkedIn/Twitter: Image URL input required.**

When preview is toggled:
- Show input field for image URL (required): "URL de la imagen *"
  - Placeholder: "https://ejemplo.com/imagen.jpg"
  - Help text: "La imagen debe ser JPEG o PNG, entre 320x320 y 1440x1440 pixeles"
  - Validate URL starts with https://
- Show optional section for carousel (expandable):
  - "Agregar mas imagenes (carrusel)" toggle
  - When expanded: list of URL inputs, "Agregar imagen" button (max 10)
  - "Eliminar" button on each extra image
  - Counter: "X de 10 imagenes"
- Below inputs: show InstagramPostPreview (single) or InstagramCarouselPreview (multiple)
- Caption preview uses contentBody (truncated to 2200 chars)

Client-side validation before publish:
- Image URL required and must be https://
- Caption <= 2200 chars
- If carousel: 2-10 images
- Show validation errors in red below the respective input

Publish flow:
- Confirm: "Publicar en Instagram?" (single) or "Publicar carrusel de X imagenes en Instagram?"
- Loading state with Loader2 spinner
- On success: toast "Publicado en Instagram" + "Post publicado exitosamente"
- On error: toast "Error al publicar" + error message

Warnings:
- Not connected: "No hay cuenta de Instagram conectada"
- Rate limited: "Limite diario alcanzado"
- Caption too long: character count in red
- No image: "Se requiere una imagen para publicar en Instagram"

All text in Spanish. Mobile responsive. Loading states on all async operations.
  </action>
  <verify>Run `cd /home/tomas/Escritorio/AIAIAI_Consulting/projects/amd/ai-marketing-department/ai-marketing-department && npx next build --no-lint 2>&1 | tail -20` to verify compilation.</verify>
  <done>PublishToInstagramButton handles image URL input, carousel management, validation, preview, and publishing. InstagramCarouselPreview shows navigable image carousel. All Spanish, responsive.</done>
</task>

</tasks>

<verification>
1. Frontend compiles without TypeScript errors
2. InstagramConnectionCard displays all status states including pending_review
3. Facebook Business requirement is clearly explained
4. PublishToInstagramButton handles single image and carousel workflows
5. InstagramPostPreview renders realistic Instagram post
6. InstagramCarouselPreview navigates between images
7. Image URL validation works client-side
8. All text in Spanish (UX-01)
9. Components responsive (UX-02)
10. Toast notifications (UX-03)
11. Loading states (UX-04)
</verification>

<success_criteria>
- IG-01: Connection card with Instagram Business account guidance
- IG-02: Single image post publishing with URL input
- IG-03: Carousel support with multiple image URLs
- IG-04: Connection status with Facebook Page name and Business requirement
- IG-05: Client-side validation (caption length, image URL, carousel count)
- UX-01: 100% Spanish text
- UX-02: Mobile responsive
- UX-03: Toast notifications
- UX-04: Loading states
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-platform-publishing/15-05-SUMMARY.md`
</output>
