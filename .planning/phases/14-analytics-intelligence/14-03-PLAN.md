---
phase: 14-analytics-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx
  - ai-marketing-department/ai-marketing-department/components/analytics/DateRangeFilter.tsx
  - ai-marketing-department/ai-marketing-department/components/analytics/ContentPerformanceTable.tsx
  - ai-marketing-department/ai-marketing-department/components/analytics/CsvExportButton.tsx
  - ai-marketing-department/ai-marketing-department/components/analytics/ContentInsightsPanel.tsx
  - ai-marketing-department/ai-marketing-department/lib/csv-export.ts
autonomous: false

# Requirement coverage mapping:
# AL-04 (A/B content insights): ContentInsightsPanel groups content by type, topic, posting time
# SC #1 (internal metrics): Page uses getAnalyticsWithDateRange for tokens, costs, agent activity
# SC #2 (LinkedIn engagement): ContentPerformanceTable uses getContentPerformance query from 14-01
# SC #3 (date filter): DateRangeFilter controls startDate/endDate for all queries
# SC #4 (CSV export): CsvExportButton uses getAnalyticsExportData query from 14-01
# SC #5 (best content): ContentPerformanceTable ranked by engagement metrics

must_haves:
  truths:
    - "User can filter analytics by date range (7 days, 30 days, custom)"
    - "User can see LinkedIn engagement metrics for published content"
    - "User can export analytics data as CSV"
    - "User can identify best-performing content by engagement"
    - "User can compare performance across content types, topics, and posting times (AL-04)"
    - "User can see internal metrics: tokens used, costs, agent activity, content created (AI-01)"
    - "All text is in Spanish"
  artifacts:
    - path: "ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx"
      provides: "Enhanced analytics page with date filters, engagement data, export, internal metrics"
      min_lines: 200
    - path: "ai-marketing-department/ai-marketing-department/components/analytics/DateRangeFilter.tsx"
      provides: "Date range selector component (7d, 30d, 90d, custom)"
      exports: ["DateRangeFilter"]
    - path: "ai-marketing-department/ai-marketing-department/components/analytics/ContentPerformanceTable.tsx"
      provides: "Table showing content ranked by engagement metrics"
      exports: ["ContentPerformanceTable"]
    - path: "ai-marketing-department/ai-marketing-department/components/analytics/ContentInsightsPanel.tsx"
      provides: "A/B content insights: performance grouped by type, topic, posting time (AL-04)"
      exports: ["ContentInsightsPanel"]
    - path: "ai-marketing-department/ai-marketing-department/components/analytics/CsvExportButton.tsx"
      provides: "Button that triggers CSV download"
      exports: ["CsvExportButton"]
    - path: "ai-marketing-department/ai-marketing-department/lib/csv-export.ts"
      provides: "Utility to convert analytics data to CSV and trigger download"
      exports: ["exportToCsv"]
  key_links:
    - from: "ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx"
      to: "convex/analytics.ts"
      via: "useQuery(api.analytics.getAnalyticsWithDateRange, { startDate, endDate }) for internal metrics (AI-01)"
      pattern: "api\\.analytics\\.getAnalyticsWithDateRange"
    - from: "ai-marketing-department/ai-marketing-department/components/analytics/ContentPerformanceTable.tsx"
      to: "convex/analytics.ts"
      via: "Data fed from useQuery(api.analytics.getContentPerformance) called in analytics page"
      pattern: "getContentPerformance"
    - from: "ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx"
      to: "convex/analytics.ts"
      via: "useQuery(api.analytics.getContentPerformance, { startDate, endDate, sortBy }) for LinkedIn engagement display"
      pattern: "api\\.analytics\\.getContentPerformance"
    - from: "ai-marketing-department/ai-marketing-department/components/analytics/ContentInsightsPanel.tsx"
      to: "convex/analytics.ts"
      via: "Uses getContentPerformance data grouped by type/topic/posting time for AL-04 insights"
      pattern: "getContentPerformance"
    - from: "ai-marketing-department/ai-marketing-department/components/analytics/CsvExportButton.tsx"
      to: "convex/analytics.ts"
      via: "useQuery(api.analytics.getAnalyticsExportData, { startDate, endDate }) for CSV data"
      pattern: "api\\.analytics\\.getAnalyticsExportData"
---

<objective>
Rebuild the analytics dashboard page with date-range filtering, LinkedIn engagement display, content performance ranking, A/B content insights, and CSV export functionality. Transform the current basic analytics view into a comprehensive marketing intelligence dashboard showing both internal metrics and LinkedIn performance.

Purpose: This is the user-facing delivery of Phase 14. Users need to see real engagement data, filter by time period, identify best content, compare content performance by type/topic/posting time (AL-04), and export data for external reporting. All 5 success criteria depend on this UI.
Output: Enhanced analytics page with 5 new components, all in Spanish
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-analytics-intelligence/14-01-SUMMARY.md
@ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx
@convex/analytics.ts
@ai-marketing-department/ai-marketing-department/components/charts/theme.ts
@ai-marketing-department/ai-marketing-department/components/ui/Card.tsx
@ai-marketing-department/ai-marketing-department/components/ui/Badge.tsx
@ai-marketing-department/ai-marketing-department/components/ui/Skeleton.tsx
@ai-marketing-department/ai-marketing-department/components/ui/Toast.tsx
@ai-marketing-department/ai-marketing-department/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DateRangeFilter, ContentPerformanceTable, ContentInsightsPanel, CsvExportButton components and csv-export utility</name>
  <files>
    ai-marketing-department/ai-marketing-department/components/analytics/DateRangeFilter.tsx,
    ai-marketing-department/ai-marketing-department/components/analytics/ContentPerformanceTable.tsx,
    ai-marketing-department/ai-marketing-department/components/analytics/ContentInsightsPanel.tsx,
    ai-marketing-department/ai-marketing-department/components/analytics/CsvExportButton.tsx,
    ai-marketing-department/ai-marketing-department/lib/csv-export.ts
  </files>
  <action>
Create a `components/analytics/` directory with four components and a utility file.

**1. `DateRangeFilter.tsx`**
"use client" component. Props: `{ value: { startDate: number; endDate: number; label: string }; onChange: (range: { startDate: number; endDate: number; label: string }) => void }`.

Renders a row of preset buttons + custom date inputs:
- "7 dias" (last 7 days)
- "30 dias" (last 30 days)
- "90 dias" (last 90 days)
- "Personalizado" (shows two date input fields when clicked)

Styling: dark theme matching existing UI (bg-zinc-900, border-zinc-800, text-zinc-300). Active button gets bg-indigo-600 text. Use Tailwind classes consistent with existing components. Buttons are pill-shaped (rounded-full px-4 py-1.5 text-sm).

For custom range: show two `<input type="date">` fields with dark styling. When dates change, call onChange with timestamps.

Export: `DateRangeFilter` as named export.

**2. `ContentPerformanceTable.tsx`**
"use client" component. Props: `{ data: Array<{ contentId: string; title: string; type: string; publishedAt?: number; engagement: { likes: number; comments: number; shares: number; impressions: number; engagement_rate?: number } | null }>; isLoading: boolean }`.

NOTE: This component receives data that was fetched using `useQuery(api.analytics.getContentPerformance, { startDate, endDate, sortBy })` from the Plan 14-01 `getContentPerformance` query. The parent analytics page makes this query call and passes the results as props.

Renders a table with columns:
- Titulo (title, truncated to 50 chars)
- Tipo (content type, with colored badge)
- Likes (with heart icon)
- Comentarios (with message-circle icon)
- Compartidos (shares, with share-2 icon)
- Impresiones (impressions, formatted with K/M)
- Tasa de Engagement (engagement_rate as percentage, colored: green > 3%, yellow 1-3%, red < 1%)

Sort by total engagement (likes+comments+shares) desc by default. Show "Sin datos de LinkedIn" in muted text for rows where engagement is null.

Use existing Card, Badge components. Dark theme table matching the existing Recent Executions table style in analytics/page.tsx. Include Skeleton loading state.

**3. `ContentInsightsPanel.tsx`** (covers AL-04: A/B content insights)
"use client" component. Props: `{ data: Array<{ contentId: string; title: string; type: string; publishedAt?: number; engagement: { likes: number; comments: number; shares: number; impressions: number; engagement_rate?: number } | null }>; isLoading: boolean }`.

This component takes the SAME data from `getContentPerformance` and groups it to provide comparative insights.

Renders three insight cards side by side (grid-cols-3 on lg, grid-cols-1 on mobile):

**Card 1: "Rendimiento por Tipo de Contenido"**
- Group content by `type` field (e.g., blog, social_linkedin, social_twitter, etc.)
- For each type: show avg engagement (likes+comments+shares), avg impressions, count of pieces
- Display as a mini bar chart or ranked list with colored badges per type
- Highlight the best-performing type with a star icon

**Card 2: "Rendimiento por Hora de Publicacion"**
- Group content by hour of day from `publishedAt` timestamp (0-23h, bucket into morning 6-12, afternoon 12-18, evening 18-24, night 0-6)
- Show avg engagement per time slot
- Highlight optimal posting time with a clock icon

**Card 3: "Tendencias por Tema"**
- Group content by type (as a proxy for topic -- full topic extraction would need NLP)
- Show which content types have improving vs declining engagement over time
- For each type, compare first half vs second half of date range engagement
- Show trending up/down arrow indicators

Use existing Card, Badge components. Dark theme. Include Skeleton loading state. All labels in Spanish.

**4. `CsvExportButton.tsx`**
"use client" component. Props: `{ startDate: number; endDate: number }`.

Button that says "Exportar CSV" with a Download icon (from lucide-react). On click:
- Calls `useQuery(api.analytics.getAnalyticsExportData, { startDate, endDate })` -- but since this is reactive, pre-fetch the data with useQuery at component level
- Calls `exportToCsv()` utility with the data
- Shows a toast "Datos exportados correctamente" on success

Use existing Button component styling or create a styled button matching the dark theme.

**5. `lib/csv-export.ts`**
Utility function `exportToCsv(data: { executions: any[]; tasks: any[]; content: any[] }, filename?: string)`.

Logic:
- Create 3 CSV sheets combined into one file with section headers
- Section 1: "## Ejecuciones" -- columns: Fecha, Agente, Estado, Tokens, Costo, Duracion
- Section 2: "## Tareas" -- columns: Fecha, Titulo, Tipo, Prioridad, Estado
- Section 3: "## Contenido" -- columns: Fecha, Titulo, Tipo, Estado, Likes, Comentarios, Compartidos, Impresiones
- Format dates as ISO strings, costs as fixed 4 decimals
- Create Blob, trigger download via hidden anchor element
- Default filename: `amd-analytics-{date}.csv`

Export: `exportToCsv` as named export.

All components MUST use Spanish labels (UX-01). All text visible to users in Spanish.
  </action>
  <verify>Run `npx next build` from the frontend directory (or `npx tsc --noEmit` for faster type check). All 5 files should compile without errors. Verify the components directory was created and files exist.</verify>
  <done>All 5 files created: DateRangeFilter with preset/custom ranges, ContentPerformanceTable with engagement columns (wired to getContentPerformance data), ContentInsightsPanel with type/time/trend comparisons (AL-04), CsvExportButton with download trigger, csv-export utility. All Spanish labels. All compile.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild analytics page with date filtering, engagement data, content insights, and CSV export</name>
  <files>ai-marketing-department/ai-marketing-department/app/(dashboard)/analytics/page.tsx</files>
  <action>
Rewrite the analytics page to integrate the new components and queries. Keep the existing visual style (dark theme, gradient header, Card components, chart components) but add:

**Page structure (top to bottom):**

1. **Header** (keep existing gradient style): "Analiticas" title (in Spanish), subtitle "Metricas de rendimiento e inteligencia de tu equipo de marketing IA."

2. **Controls row**: `<DateRangeFilter>` on the left, `<CsvExportButton>` on the right. Default to last 30 days.

3. **Overview Stats** (4 cards, keep existing MetricCard pattern): Executions, Success Rate, Tokens Used, Total Cost. Now pass `startDate`/`endDate` to the query. Use `useQuery(api.analytics.getAnalyticsWithDateRange, { startDate, endDate })` instead of the old `api.functions.getAnalyticsOverview`. This query returns internal metrics covering AI-01 (tokens, costs, agent activity, content created).

4. **Charts row** (keep existing chart components):
   - Tasks by Day bar chart (from getAnalyticsWithDateRange.tasksByDay -- this is the AI-02 daily rollup data)
   - Performance Trends area chart (from query results)

5. **NEW: Content Performance section** (full-width Card):
   - Title: "Rendimiento de Contenido" with a Trophy icon
   - Use `<ContentPerformanceTable>` fed by `useQuery(api.analytics.getContentPerformance, { startDate, endDate, sortBy: "engagement" })` -- this is the explicit wiring to the getContentPerformance query from Plan 14-01
   - Shows published content ranked by LinkedIn engagement (SC #2, SC #5)

6. **NEW: Content Insights section** (full-width Card, covers AL-04):
   - Title: "Insights de Contenido" with a Lightbulb icon
   - Use `<ContentInsightsPanel>` fed by the SAME getContentPerformance data (pass contentPerformanceData prop)
   - Groups content by type, posting time, and topic trends for A/B comparison

7. **Agent Performance row** (2 cards, keep existing style):
   - Top Agents by Executions (keep existing horizontal bars)
   - Cost Distribution donut (keep existing)

8. **Recent Executions table** (keep existing, but date-filtered)

9. **Additional Stats row** (3 cards, keep existing: Avg Duration, Avg Cost/Execution, Avg Tokens/Execution)

**Query wiring (explicit mapping):**
- `useQuery(api.analytics.getAnalyticsWithDateRange, { startDate, endDate })` -- provides overview stats, tasksByDay, topAgents, recentExecutions (AI-01, AI-02)
- `useQuery(api.analytics.getContentPerformance, { startDate, endDate, sortBy: "engagement" })` -- provides content performance data for ContentPerformanceTable AND ContentInsightsPanel (AL-01 through AL-04)
- `<CsvExportButton>` internally uses `useQuery(api.analytics.getAnalyticsExportData, { startDate, endDate })`

**State management:**
- Use `useState` for date range: `{ startDate: number, endDate: number, label: string }`. Default: last 30 days.
- Pass startDate/endDate to all queries. All queries are reactive via Convex useQuery.
- The DateRangeFilter onChange updates the state, which triggers re-render and new queries.

**Loading states:**
- Use existing SkeletonStat, SkeletonChart, SkeletonTable for loading (UX-04)
- Show skeleton when any query returns undefined

**Spanish translation (UX-01):**
- Replace all English text: "Analytics" -> "Analiticas", "Executions" -> "Ejecuciones", "Success Rate" -> "Tasa de Exito", "Tokens Used" -> "Tokens Usados", "Total Cost" -> "Costo Total", "Tasks by Day" -> "Tareas por Dia", "Performance Trends" -> "Tendencias de Rendimiento", "Top Agents by Executions" -> "Top Agentes por Ejecuciones", "Cost Distribution" -> "Distribucion de Costos", "Recent Executions" -> "Ejecuciones Recientes", "Avg Duration" -> "Duracion Promedio", "Avg Cost/Execution" -> "Costo Promedio/Ejecucion", "Avg Tokens/Execution" -> "Tokens Promedio/Ejecucion"
- Date formatting: use "es-ES" locale
- All table headers in Spanish

**Mobile responsive (UX-02):**
- Controls row: stack vertically on mobile (flex-col on sm, flex-row on md)
- Stats grid: 1 col on mobile, 2 on md, 4 on lg (keep existing responsive classes)
- Content performance table: horizontal scroll on mobile (overflow-x-auto)
- Content insights: 1-col stack on mobile, 3-col on lg

**Keep the existing MetricCard component** at the bottom of the file (or move it inline). Update it if needed but preserve the sparkline/trend functionality.

**Remove mock data**: The old page had `Math.random()` for trends and mock cost distribution. Replace all mock data with real data from the new queries. If trend data isn't available yet (no historical comparison), show trend as undefined (hide the TrendIndicator).

Keep the `useEffect` for `completeStep({ step: "analyticsViewed" })` -- that's the onboarding integration.
  </action>
  <verify>Run `npx tsc --noEmit` from the frontend directory. No TypeScript errors. Then start the dev server (`npm run dev`) and visit `/analytics` -- the page should render without runtime errors (even if data is empty, it should show empty states gracefully).</verify>
  <done>Analytics page rebuilt with: date range filter (7d/30d/90d/custom), content performance table with LinkedIn engagement (wired to getContentPerformance), content insights panel with type/time/trend comparison (AL-04), CSV export button, all Spanish labels, mobile responsive, loading skeletons, no mock data. Internal metrics (AI-01) displayed via getAnalyticsWithDateRange.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify analytics dashboard UX</name>
  <what-built>
Complete analytics dashboard overhaul with:
- Date range filtering (7 dias / 30 dias / 90 dias / Personalizado)
- LinkedIn engagement data display for published content (from getContentPerformance query)
- CSV export functionality (from getAnalyticsExportData query)
- Content performance ranking table
- Content insights panel: performance by type, posting time, topic trends (AL-04)
- Internal metrics: tokens, costs, agent activity (AI-01 via getAnalyticsWithDateRange)
- All text in Spanish
- Mobile responsive design
  </what-built>
  <how-to-verify>
1. Navigate to http://localhost:3000/analytics
2. Verify the page loads without errors and shows Spanish labels
3. Click each date range preset (7 dias, 30 dias, 90 dias) -- stats should update
4. Click "Personalizado" -- verify date pickers appear
5. Check the "Rendimiento de Contenido" section -- should show content table (may be empty if no published content with LinkedIn engagement yet, which is OK -- verify it shows "Sin datos" gracefully)
6. Check the "Insights de Contenido" section -- should show 3 insight cards (by type, by time, by trend). May show "Sin datos suficientes" if not enough content exists yet.
7. Click "Exportar CSV" -- a CSV file should download
8. Open the CSV -- verify it has section headers in Spanish and data columns
9. Resize browser to mobile width -- verify layout stacks properly
10. Verify loading skeletons appear briefly when switching date ranges
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Analytics page renders without errors at /analytics
2. Date range filter works for all presets and custom range
3. All text is in Spanish (no English labels remain)
4. Content performance table displays LinkedIn engagement data (wired to getContentPerformance from 14-01)
5. Content insights panel shows comparisons by type, posting time, trends (AL-04)
6. CSV export downloads a file with correct data (wired to getAnalyticsExportData from 14-01)
7. Internal metrics visible: tokens, costs, agent activity, content count (AI-01 via getAnalyticsWithDateRange from 14-01)
8. Mobile responsive layout works (1-col on small screens)
9. Loading skeletons appear during data fetching
10. Empty states handled gracefully (no crashes with zero data)
</verification>

<success_criteria>
- User can view dashboard showing tokens used, costs, agent activity, and content created over time (SC #1 / AI-01 via getAnalyticsWithDateRange)
- User can see LinkedIn post engagement for published content (SC #2 via getContentPerformance)
- User can filter analytics by date range: 7 days, 30 days, custom (SC #3)
- User can export analytics data as CSV (SC #4 via getAnalyticsExportData)
- User can identify best-performing content by engagement metrics (SC #5)
- User can compare performance across content types, topics, and posting times (AL-04 via ContentInsightsPanel)
- All UX requirements met: Spanish (UX-01), mobile (UX-02), toasts (UX-03), loading states (UX-04)
</success_criteria>

<output>
After completion, create `.planning/phases/14-analytics-intelligence/14-03-SUMMARY.md`
</output>
