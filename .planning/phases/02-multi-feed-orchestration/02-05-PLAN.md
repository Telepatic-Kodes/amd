---
phase: 02-multi-feed-orchestration
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Dashboard shows feeds with correct status"
    - "Add feed creates new feed and it appears in list"
    - "Pause/resume changes feed status"
    - "Manual sync triggers and updates last sync time"
    - "Delete removes feed and its items"
    - "10+ feeds can sync without timeout"
---

<objective>
Verify the complete multi-feed orchestration system end-to-end.

Purpose: Confirm all requirements (FEED-01, FEED-03, FEED-04, FEED-05, SYNC-01, SYNC-04, DASH-01, DASH-02, DASH-03, DASH-04) are met before marking phase complete.

Output: Verified working system ready for Phase 3 integration.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete multi-feed orchestration system with:
- Backend: Feed CRUD mutations, public queries, fan-out orchestration, cron jobs, rate limiting
- Frontend: /feeds dashboard page with add form, feed cards, status indicators, sync buttons
  </what-built>
  <how-to-verify>
**Preparation:**
1. Ensure both Convex backend and Next.js frontend are running:
   - Terminal 1: `cd /home/tomas/Escritorio/amd && npx convex dev`
   - Terminal 2: `cd /home/tomas/Escritorio/amd/ai-marketing-department/ai-marketing-department && npm run dev`

**Test 1: Navigation (DASH-01)**
1. Open http://localhost:3000
2. Click "Feeds" in sidebar
3. Verify /feeds page loads with empty state or existing feeds

**Test 2: Add Feed (FEED-01, DASH-01)**
1. Click "Add Feed" button
2. Enter test feed URL: `https://blog.google/rss/`
3. Enter name: "Google Blog"
4. Select category: "technical"
5. Select frequency: "daily"
6. Click "Add Feed"
7. Verify feed appears in grid with "active" status

**Test 3: Add More Feeds (for scaling test)**
Add at least 3 more feeds to test fan-out:
- `https://feeds.feedburner.com/TechCrunch/` - "TechCrunch" - industry
- `https://rss.nytimes.com/services/xml/rss/nyt/Technology.xml` - "NYT Tech" - news
- `https://www.wired.com/feed/rss` - "Wired" - technical

**Test 4: Manual Sync (DASH-04, SYNC-04)**
1. Click the sync button (refresh icon) on a feed card
2. Check Convex logs (dashboard.convex.dev -> Logs tab)
3. Verify fetchFeed action was scheduled and executed
4. Verify feed's "Last sync" time updates
5. Verify items appear when clicking on the feed

**Test 5: Pause/Resume (FEED-03, DASH-02)**
1. Click pause button on an active feed
2. Verify status changes to "paused"
3. Verify sync button becomes disabled
4. Click play button to resume
5. Verify status returns to "active"

**Test 6: Delete Feed (FEED-04)**
1. Click delete button on a test feed
2. Confirm deletion in dialog
3. Verify feed disappears from grid
4. Verify items were also deleted (check Convex dashboard)

**Test 7: View Feed Items (DASH-03)**
1. Click on a synced feed card
2. Verify detail panel opens on the right
3. Verify feed items are displayed
4. Verify "Recent Items" section shows items from all feeds

**Test 8: Status Indicators (DASH-02, SYNC-06)**
1. Verify active feeds show green indicator
2. Verify paused feeds show yellow indicator
3. Verify error count displays correctly
4. If any feeds have errors, verify error message is shown

**Test 9: Filters (FEED-05)**
1. Use status filter to show only active feeds
2. Use category filter to show only technical feeds
3. Use search to find a specific feed by name
4. Verify filters work correctly

**Test 10: Cron Jobs (SYNC-01)**
1. Go to Convex dashboard -> Cron Jobs tab
2. Verify three feed sync crons are registered:
   - sync-daily-feeds
   - sync-hourly-feeds
   - sync-weekly-feeds
  </how-to-verify>
  <resume-signal>
Type "verified" if all tests pass, or describe any issues found.

If issues found, provide:
- Which test failed
- Expected behavior
- Actual behavior
- Any error messages from browser console or Convex logs
  </resume-signal>
</task>

</tasks>

<verification>
All 10 test scenarios pass as described above.
</verification>

<success_criteria>
- [ ] All 10 verification tests pass
- [ ] No console errors in browser
- [ ] No uncaught errors in Convex logs
- [ ] System handles 10+ feeds without timeout
- [ ] User can complete full CRUD cycle on feeds
- [ ] Dashboard displays accurate feed status and sync info
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-feed-orchestration/02-05-SUMMARY.md`

This checkpoint completes Phase 2. Upon success, update STATE.md and ROADMAP.md to mark Phase 2 complete.
</output>
