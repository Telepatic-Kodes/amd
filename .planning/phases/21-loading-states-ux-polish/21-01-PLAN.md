# Plan 21-01: Loading.tsx Skeletons + Page Transitions + Button Loading States

## Requirements
- LOAD-01: Skeleton screens for all pages (no blank white screens)
- LOAD-03: Button loading states on all form submissions (disabled + spinner)
- LOAD-04: Page transition animations (Framer Motion fade/slide)

## Current State
- `Skeleton.tsx` exists with 8 variants (Skeleton, SkeletonCard, SkeletonStat, SkeletonChart, SkeletonDonut, SkeletonTable, SkeletonList, SkeletonGrid)
- Dashboard home page has inline skeletons (HeroMetricSkeleton, etc.)
- **Zero loading.tsx** files exist in any route
- Framer Motion installed (v12.29.2) but not used for page transitions
- Some forms have button loading states, others don't

## Tasks

### 1. Create loading.tsx for all dashboard routes
Next.js loading.tsx provides instant skeleton feedback while page component loads.

Routes to cover (10 routes):
- `app/(dashboard)/loading.tsx` — SkeletonStat x4 + SkeletonChart + SkeletonTable
- `app/(dashboard)/agents/loading.tsx` — SkeletonGrid 6 items
- `app/(dashboard)/content/loading.tsx` — SkeletonGrid 6 items
- `app/(dashboard)/analytics/loading.tsx` — SkeletonStat x4 + SkeletonChart x2
- `app/(dashboard)/brand/loading.tsx` — SkeletonCard + SkeletonList
- `app/(dashboard)/campaigns/loading.tsx` — SkeletonGrid 4 items
- `app/(dashboard)/settings/loading.tsx` — SkeletonCard x3
- `app/(dashboard)/results/loading.tsx` — SkeletonStat x3 + SkeletonChart
- `app/(dashboard)/control-center/loading.tsx` — SkeletonStat x3 + SkeletonTable
- `app/(dashboard)/feeds/loading.tsx` — SkeletonList 5 items

### 2. Add PageTransition wrapper component
Create `components/ui/PageTransition.tsx` with Framer Motion fade animation. Wrap each page's content for smooth route transitions.

### 3. Create LoadingButton component
Create `components/ui/LoadingButton.tsx` — reusable button with built-in loading state: disabled + Loader2 spinner + text swap.

## Files Created
- 10 loading.tsx files
- `components/ui/PageTransition.tsx`
- `components/ui/LoadingButton.tsx`

## Verification
- All routes show skeleton during load instead of blank screen
- Tests pass: `npx vitest run`
