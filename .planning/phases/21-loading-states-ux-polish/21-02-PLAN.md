# Plan 21-02: Agent Execution Progress + Empty States for Key Pages

## Requirements
- LOAD-02: Agent execution progress indicator with stage updates
- EMPTY-01: Content library empty state with CTA
- EMPTY-02: Agents page empty state with quick-start
- EMPTY-03: Analytics empty state with sample preview
- EMPTY-04: Campaigns empty state with campaign wizard preview

## Current State
- `QuickExecuteModal.tsx` shows only a spinner + "Ejecutando..." — no progress stages
- `EmptyState.tsx` exists with generic variants but missing page-specific CTAs
- Content page has EmptyContent but no action button
- Agents page has EmptyAgents but no quick-start guide
- Analytics/Campaigns pages have no empty state handling

## Tasks

### 1. Enhance QuickExecuteModal with execution stages
Add a multi-step progress indicator showing Spanish stage labels:
1. "Preparando agente..." (0-2s)
2. "Analizando brief..." (2-5s)
3. "Generando contenido..." (5-15s)
4. "Finalizando..." (15s+)
Use Framer Motion for animated step transitions.

### 2. Add empty states with CTAs to 4 key pages
Update each page to show a proper empty state when data array is empty:

- **Content** (`content/page.tsx`): "Crea tu primer contenido" CTA → opens UploadContentForm
- **Agents** (`agents/page.tsx`): "Los 37 agentes están listos" + "Ejecutar primer agente" CTA
- **Analytics** (`analytics/page.tsx`): "Publica contenido para ver estadísticas" + link to content
- **Campaigns** (`campaigns/page.tsx`): "Crea tu primera campaña" CTA → opens create campaign flow

## Files Modified
- `components/dashboard/QuickExecuteModal.tsx` — add execution stages
- `app/(dashboard)/content/page.tsx` — add empty state with CTA
- `app/(dashboard)/agents/page.tsx` — add empty state with CTA
- `app/(dashboard)/analytics/page.tsx` — add empty state with CTA
- `app/(dashboard)/campaigns/page.tsx` — add empty state with CTA

## Verification
- Agent execution shows animated progress stages
- Each of 4 pages shows proper empty state when no data
- Tests pass: `npx vitest run`
